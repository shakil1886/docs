---

title: Workflow
description: "The Workflow Page is where you create, manage, and execute test workflows for your web application. It offers both manual <b> workflow </b> creation and <b> AI-powered </b> workflow generation, allowing you to customize or automate your testing process efficiently."

---

## Creating a Workflow

### Manually Creating a Workflow

You can create a workflow manually using the <b> Workflow</b> button for you.

<img src="/images/createWorkflow.png" className="w-full rounded-xl shadow-xl" />

Add workflow <b> Description </b> and set <b>prompt</b> to get expected results.{" "}

### Using AI to Generate a Workflow

Or <b>Nota AI's</b> agent generate workflows for you. Click the "Generate Coverage" button (opens a modal).

<img
  src="/images/AiGenerateWorkflow.png"
  className="w-full rounded-xl shadow-xl"
/>
Enter a <b>short description</b> of the test scenario you want to automate.
<li> Example: "Test the login functionality with valid and invalid credentials." </li>

(Optional) Provide a detailed prompt for more precise workflow generation.
<li> Example: "Navigate to the login page, enter valid credentials, click login, verify successful login. Then enter incorrect credentials, verify error message appears."</li>

Select an <b> environment </b> from the dropdown list

<li> The selected environment defines the application where the generated workflow will run. </li>

Click "Generate Workflows", and the AI agent will create the workflow.

Now, you’re ready to <b>execute or automate</b> the AI-generated workflow to enhance your testing process! 🚀

## Managing Your Workflows

Once you have created workflows, you can manage them directly from the <b>Workflow Page</b>.

### Selecting & Executing Multiple Workflows

Select multiple workflows by checking the boxes next to them.

<img
  src="/images/workflow-page.png"
  className="w-full rounded-xl shadow-xl"
/>

Click "RUN WORKFLOW" to run all selected workflows at once.

### Updating & Deleting a Workflow
<ul>
<li>
Click on the workflow you want to edit.
</li>
<li>
Modify test steps and click any where in the app.
</li>
<li>
Click the "Delete" button to remove them permanently.
</li>
</ul>